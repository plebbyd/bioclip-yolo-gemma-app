# Sage / pywaggle (waggle.data.vision imports ``ffmpeg`` → PyPI package ffmpeg-python)
pywaggle>=0.56.3
ffmpeg-python>=0.2.0

# Core CV
numpy>=1.24
Pillow>=10.0
opencv-python-headless>=4.8
huggingface_hub>=0.20

# Models: YOLO + BioCLIP / BioCLIP 2 / BioCLIP 2.5 (OpenCLIP).
#   torch + torchvision install CPU wheels from PyPI for both amd64 and arm64, so
#   the image builds multi-arch in ECR. Inference uses the GPU automatically if a
#   CUDA-enabled torch is present instead (e.g. the Jetson image / Dockerfile.jetson-thor).
torch
torchvision
ultralytics>=8.0
open_clip_torch>=2.24
